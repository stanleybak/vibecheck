PIP RELEASE TODO (autotune version = branch `main`)
====================================================
Keep this file untracked. Do the work in a SEPARATE checkout/worktree of
`main` (this tree stays on clean-slate for the vc2 build).

0. Name: PyPI name `vibecheck` is TAKEN (0.0.5 by someone else).
   Use distribution name `vibecheck-nn`; import package and console script
   stay `vibecheck`. Update the standard CLI's --version lookup:
   importlib.metadata.version("vibecheck") -> "vibecheck-nn"
   (src/vibecheck/cli_standard.py).

1. REQUIRED CODE CHANGE - bundled configs:
   config_loader.py:23 resolves configs/ by climbing OUT of the package
   (dirname^3/configs) -> broken in an installed wheel.
   - move configs/ -> src/vibecheck/configs/
   - resolve via importlib.resources.files("vibecheck") / "configs"
   - pyproject: [tool.setuptools.package-data] vibecheck = ["configs/*.yaml"]
   - grep for other repo-root-relative paths (tests/paths.yaml is test-only,
     fine).

2. pyproject metadata:
   - name = "vibecheck-nn", bump version (0.1.0 ok for first release)
   - authors, license = {file = "LICENSE"} (GPLv3), readme = "README.md"
   - [project.urls] Homepage/Repository = github.com/stanleybak/vibecheck-nn
   - classifiers: Python 3.10-3.12 (capped by onnx/ort pins), License, Topic.

3. Dependency decisions:
   - keep exact pins onnx==1.21.0 / onnxruntime==1.26.0 / vnnlib==1.0.2
     (scorer-replay parity); document the why in README.
   - torch unpinned; README note about install size.
   - DECIDE: gurobipy required vs optional extra [gurobi] with graceful
     degradation message (pip wheel ships size-limited license either way).
   - confirm vendored vnncomp_cex_v2.py provenance/license header (GPLv3
     redistribution).

4. Build + local verification:
   pip install build twine; python -m build; twine check dist/*
   Fresh venv, install the WHEEL, run FROM OUTSIDE THE REPO:
     vibecheck --version
     vibecheck verify <query> --network N=<net> --timeout 60
     an auto-config (no --config) case  <- catches the configs bug
   Optionally run tests/ against the installed package (no PYTHONPATH=src).

5. TestPyPI dry run:
   twine upload -r testpypi dist/*
   pip install -i https://test.pypi.org/simple/ \
       --extra-index-url https://pypi.org/simple vibecheck-nn

6. Publish (pick one):
   a) manual: PyPI account + 2FA + API token; twine upload dist/*
   b) RECOMMENDED: Trusted Publishing - register a publisher on PyPI for
      github.com/stanleybak/vibecheck-nn + a GitHub Actions release workflow
      (pypa/gh-action-pypi-publish); then: push tag vX.Y.Z -> CI builds,
      tests, publishes. No long-lived tokens.

7. Bookkeeping:
   - tag the release commit on main; GitHub release notes
   - releases from main only; clean-slate merges later as a version bump
     (vc2 imports vibecheck.* so the same packaging covers both packages
     under src/).
